source 'https://rubygems.org'

gem 'activerecord'
gem 'bio'
gem 'gene_ontology'

# gruff depends on rmagick. installing the rmagick gem will fail on Mavericks unless run like so:
#    sudo ARCHFLAGS=-Wno-error=unused-command-line-argument-hard-error-in-future bundle install
gem 'gruff'

gem 'rspec'
gem 'trollop'
